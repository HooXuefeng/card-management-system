# 饭卡管理系统项目总结

## 项目概述
饭卡管理系统是一个基于前后端分离架构的现代化Web应用，用于管理用户的饭卡信息、充值记录、消费记录等功能。系统提供了直观的用户界面和完善的功能模块，方便用户进行饭卡管理和查询。

## 技术栈

### 前端技术栈
| 技术/框架 | 版本 | 用途 |
|---------|-----|-----|
| Vue 3 | 3.3.4 | 前端框架 |
| Vite | 4.4.5 | 构建工具 |
| Vue Router | 4.2.5 | 路由管理 |
| Element Plus | 2.13.0 | UI组件库 |
| Axios | 1.13.2 | HTTP请求库 |
| ECharts | 6.0.0 | 图表可视化 |
| @element-plus/icons-vue | 2.3.2 | 图标库 |

### 后端技术栈
| 技术/框架 | 版本 | 用途 |
|---------|-----|-----|
| Spring Boot | 3.0.6 | 后端框架 |
| Java | 17 | 开发语言 |
| MySQL | - | 数据库 |
| Redis | - | 缓存 |
| Spring Security | - | 安全管理 |
| Spring Data Redis | - | Redis数据访问 |
| Spring JDBC | - | 数据库操作 |
| Lombok | - | 代码简化 |

## 项目结构

### 前端项目结构
```
card-frontend/
├── src/
│   ├── views/            # 页面组件
│   │   ├── Login.vue     # 登录页面
│   │   ├── Home.vue      # 首页
│   │   ├── Password.vue  # 密码修改
│   │   ├── CardDetail.vue# 卡片详情
│   │   └── Reports.vue   # 统计报表
│   ├── router/           # 路由配置
│   │   └── index.js      # 路由定义和守卫
│   ├── utils/            # 工具函数
│   │   └── request.js    # Axios配置
│   ├── App.vue           # 根组件
│   └── main.js           # 入口文件
├── package.json          # 依赖配置
└── vite.config.js        # Vite配置
```

### 后端项目结构
```
card-backend/
├── src/main/
│   ├── java/com/example/card/
│   │   ├── CardBackendApplication.java # 启动类
│   │   ├── controller/                 # 控制器
│   │   │   └── UserController.java     # 用户控制器
│   │   ├── service/                    # 服务层
│   │   │   └── UserService.java        # 用户服务
│   │   └── entity/                     # 实体类
│   │       └── UserInfo.java           # 用户信息实体
│   └── resources/
│       └── application.properties      # 配置文件
└── pom.xml                              # Maven依赖
```

## 功能模块

### 1. 用户认证模块
- 用户登录功能，支持账号密码验证
- 记住我功能，可选token持久化方式（localStorage/sessionStorage）
- 路由守卫，保护需要认证的页面
- 登录过期自动重定向到登录页

### 2. 饭卡信息管理
- 查看饭卡基本信息（卡号、余额、状态、注册日期）
- 饭卡挂失/解冻功能
- 饭卡状态显示（正常、冻结、禁用）

### 3. 充值管理
- 饭卡充值功能
- 充值金额验证（必须大于0）
- 卡片状态验证（只有正常状态可充值）
- 充值记录查询

### 4. 消费管理
- 消费记录查询
- 消费趋势图表可视化（周/月数据）
- 消费金额统计

### 5. 统计报表
- 充值记录汇总
- 消费记录汇总
- 消费趋势分析

## API接口设计

### 1. 用户认证接口
| 接口路径 | 方法 | 功能 | 参数 | 返回值 |
|---------|-----|-----|-----|-------|
| /user/login | POST | 用户登录 | studentId, password | {code, msg, data} |

### 2. 饭卡信息接口
| 接口路径 | 方法 | 功能 | 参数 | 返回值 |
|---------|-----|-----|-----|-------|
| /api/user/info | GET | 查询饭卡信息 | - | 饭卡信息对象 |
| /api/user/status/change | POST | 修改饭卡状态 | - | 修改结果 |

### 3. 充值接口
| 接口路径 | 方法 | 功能 | 参数 | 返回值 |
|---------|-----|-----|-----|-------|
| /api/user/recharge | POST | 饭卡充值 | amount | 充值结果 |
| /api/user/recharge/records | GET | 查询充值记录 | - | 充值记录列表 |

### 4. 消费接口
| 接口路径 | 方法 | 功能 | 参数 | 返回值 |
|---------|-----|-----|-----|-------|
| /api/user/consume/records | GET | 查询消费记录 | - | 消费记录列表 |

## 核心功能实现

### 1. 用户认证流程
```
1. 用户输入账号密码登录
2. 前端发送请求到后端登录接口
3. 后端验证账号密码，返回用户信息
4. 前端根据"记住我"选项将token存储在localStorage或sessionStorage
5. 登录成功后跳转到首页
6. 首页组件获取token并加载用户数据
```

### 2. 路由守卫实现
```javascript
router.beforeEach((to, from, next) => {
  // 同时检查localStorage和sessionStorage中的token
  const token = localStorage.getItem('token') || sessionStorage.getItem('token')
  
  // 只有当页面需要认证且没有token时，才提示登录
  if (to.meta.requiresAuth && !token) {
    alert('请先登录！')
    next('/login')
  } else {
    next()
  }
})
```

### 3. HTTP请求拦截器
```javascript
// 请求拦截器：添加token到请求头
service.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token') || sessionStorage.getItem('token')
    if (token) {
      config.headers['Authorization'] = `Bearer ${token}`
    }
    return config
  },
  error => {
    console.error('请求错误:', error)
    return Promise.reject(error)
  }
)

// 响应拦截器：统一处理未登录
service.interceptors.response.use(
  response => {
    return response.data
  },
  error => {
    if (error.response && error.response.status === 401) {
      if (!error.config.url.includes('/login')) {
        localStorage.removeItem('token')
        sessionStorage.removeItem('token')
        alert('登录已过期，请重新登录')
        window.location.href = '/login'
      }
    }
    return Promise.reject(error)
  }
)
```

### 4. 消费趋势图表
使用ECharts实现消费趋势图表，支持周/月数据切换，展示消费金额变化趋势。

## 页面设计

### 1. 登录页面
- 简洁的登录表单设计
- 支持记住我功能
- 忘记密码链接
- 响应式布局，适配不同屏幕尺寸

### 2. 首页
- 顶部导航栏，显示系统名称和用户信息
- 概览卡片区域，展示余额、状态、本月充值和消费
- 我的饭卡区域，显示饭卡详情和操作按钮
- 充值模块，支持饭卡充值
- 消费趋势图表，可视化消费数据
- 充值记录和消费记录列表

## 开发环境搭建

### 前端环境搭建
1. 安装Node.js
2. 进入前端项目目录：`cd card-frontend`
3. 安装依赖：`npm install`
4. 启动开发服务器：`npm run dev`
5. 访问：`http://localhost:5174/`

### 后端环境搭建
1. 安装JDK 17
2. 安装MySQL和Redis
3. 进入后端项目目录：`cd card-backend`
4. 安装依赖：`mvn install`
5. 启动应用：`mvn spring-boot:run`
6. API接口地址：`http://localhost:8080/`

## 项目亮点

1. **前后端分离架构**：采用现代化的前后端分离架构，提高开发效率和系统可维护性

2. **完善的用户认证**：支持token持久化选择，提供安全的用户认证机制

3. **直观的数据可视化**：使用ECharts实现消费趋势图表，方便用户查看消费情况

4. **响应式设计**：页面适配不同屏幕尺寸，提供良好的移动端体验

5. **统一的错误处理**：前后端都实现了统一的错误处理机制，提高系统稳定性

6. **良好的代码结构**：代码组织清晰，便于维护和扩展

## 遇到的问题及解决方案

### 1. 登录成功后不跳转问题
**问题**：用户登录成功后，页面显示"登录成功，即将跳转到首页！"提示，但实际上并没有跳转到首页。

**原因**：Home.vue组件中只从localStorage获取token，而当用户不勾选"记住我"时，token会存储在sessionStorage中。

**解决方案**：修改Home.vue中的token获取逻辑，同时检查localStorage和sessionStorage：
```javascript
const token = localStorage.getItem('token') || sessionStorage.getItem('token')
```

**修复的位置**：
- getCardInfo函数
- getRechargeRecords函数
- getConsumeRecords函数
- handleRecharge函数
- handleStatusChange函数
- onMounted生命周期钩子

## 未来改进方向

1. **添加更多功能模块**：如用户管理、权限管理、系统设置等

2. **优化用户体验**：添加更多交互效果和动画

3. **增强安全性**：添加更多安全措施，如密码强度验证、验证码等

4. **性能优化**：优化页面加载速度和响应时间

5. **添加测试用例**：完善单元测试和集成测试

6. **文档完善**：补充更多开发文档和使用说明

## 总结

饭卡管理系统是一个功能完善、架构清晰的现代化Web应用。系统采用前后端分离架构，使用了主流的技术栈，提供了直观的用户界面和完善的功能模块。通过本项目的开发，实现了饭卡信息管理、充值管理、消费管理等核心功能，为用户提供了便捷的饭卡管理服务。